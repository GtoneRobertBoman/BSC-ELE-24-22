#ifdef CIRCLE_H
 #define CIRCLE_H

namespace shape{
    class circle {
        private:
            float radius;
        public:
            circle ();
            circle (radius2);
            ~radius();   

            void setRadius(radius3);
            void getRadius()const;

    };
 }
 #endif